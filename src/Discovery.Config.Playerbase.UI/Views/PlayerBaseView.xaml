<UserControl x:Class="Discovery.Config.Playerbase.UI.Views.PlayerBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Discovery.Config.Playerbase.UI.Views"
             xmlns:ViewModels="clr-namespace:Discovery.Config.Playerbase.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type ViewModels:PlayerBaseViewModel}, IsDesignTimeCreatable=True}">
    <Grid>

        <Expander Header="Modules">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ComboBox Grid.Column="0"
                          x:Name="recipeComboBox" 
                          ItemsSource="{Binding Path=Recipes}" 
                          DisplayMemberPath="Nickname" 
                          SelectedItem="{Binding SelectedRecipe}"/>

                <Button Grid.Column="1" 
                        Content="Add Module" 
                        Command="{Binding Path=AddModuleCommand}"
                        CommandParameter="{Binding ElementName=recipeComboBox, Path=SelectedItem}"/>

                <Button Grid.Column="2" 
                        Content="Remove Selected"
                        VerticalContentAlignment="Bottom"
                        Command="{Binding Path=RemoveModuleCommand}"
                        CommandParameter="{Binding ElementName=modulesListView, Path=SelectedItem}"/>

                <ListView Grid.ColumnSpan="3"
                          Grid.Row="1"
                          x:Name="modulesListView"
                          ItemsSource="{Binding Path=PlayerBase.Modules}"
                          SelectionMode="Single">
                          <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Nickname" DisplayMemberBinding="{Binding Nickname}" Width="200"/>
                            <GridViewColumn Header="InfoText" DisplayMemberBinding="{Binding InfoText}" Width="250"/>
                            <GridViewColumn Header="Craft Lists" DisplayMemberBinding="{Binding CraftLists}" Width="200"/>
                        </GridView>
                    </ListView.View>
                </ListView>

            </Grid>
        </Expander>
    </Grid>
</UserControl>
