<Window x:Class="Discovery.Delivery.Debugger.DebuggerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Converters="clr-namespace:Discovery.Delivery.Debugger.Converters"
        xmlns:ViewModels="clr-namespace:Discovery.Delivery.Debugger.ViewModels"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="OCR Debugger" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <ViewModels:DebuggerViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <Converters:BitmapToImageSourceConverter x:Key="ImageSourceConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Controls -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="0" HorizontalAlignment="Left">
            <Button Content="Choose Image" Margin="0,0,5,0" Command="{Binding SelectImageCommand}" Width="120"/>
            <Button Content="Start OCR" Margin="5,0" Command="{Binding AnalyseCommand}" Width="120" CommandParameter="{Binding Path=SelectedImage}"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Image Preview -->
            <Border BorderBrush="Gray" BorderThickness="1" Margin="0 0 10 0">
                <Image Source="{Binding SelectedImage, Converter={StaticResource ResourceKey=ImageSourceConverter}}" 
                       Stretch="Uniform" />
            </Border>

            <!-- PropertyGrid -->
            <xctk:PropertyGrid Grid.Column="1" SelectedObject="{Binding AnalyseResult}" PreparePropertyItem="PropertyGrid_PreparePropertyItem"/>
        </Grid>
    </Grid>
</Window>
